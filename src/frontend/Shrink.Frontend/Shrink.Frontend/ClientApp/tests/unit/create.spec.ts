import { expect } from 'chai';
import { shallowMount } from '@vue/test-utils';
import Create from '@/components/Create.vue';

describe('Create.vue', () => {
  describe('try parse url', () => {
    it('should return ture', () => {

    });
  });
});
